server:
  port: 8082
spring:
  application:
    name: provider
#  kafka:
#    bootstrap-servers: localhost:9092
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: "*"
env:
  dev
